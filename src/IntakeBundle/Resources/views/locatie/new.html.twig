{% extends "@Intake/base.html.twig" %}

{% block body %}
    {% if is_granted("ROLE_ADMIN") %}
    <h1>Locatie creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('locatie_index') }}">Back to the list</a>
        </li>
    </ul>
    {% else %}
        <p>je bent geen admin.</p>
    {% endif %}
{% endblock %}
