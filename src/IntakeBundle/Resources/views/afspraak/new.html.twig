{% extends "@Intake/base.html.twig" %}

{% block body %}
    <div class="flex-column full-width">
        <h1>Afspraak creation</h1>

        {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create"/>
        {{ form_end(form) }}

        <ul>
            <li>
                <a href="{{ path('afspraak_index') }}">Back to the list</a>
            </li>
        </ul>
    </div>
{% endblock %}
